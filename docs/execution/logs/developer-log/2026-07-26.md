# 2026-07-26

## Overview
- Done: Today responsive shell shipped (70%/50% nav stages, fixed Timeline scroll). Tuned Soft Indigo field focus globally on Input/Textarea (`primary/40` border, `ring-2` @ `/40`, 150ms; hover lightens fill only, keeps focus chrome).
- Still open: Right-drawer time AM/PM picker + related drawer polish on `tweak/right-drawer-time-ampm-picker` (uncommitted focus work included).
- Blockers: None.

## Timeline
08:33 — Ship Today responsive shell: 70%/50% nav stages, fixed Timeline scroll.
08:34 — Merge `tweak/today-responsive-timeline`; note merge hash in docs.
09:55 — Task Details title focus vs description glow mismatch (investigation).
10:00 — Iterate writing-field hover/focus (avoid darken, double glow, white flash).
10:12 — Apply same writing-field treatment to Notes search.
10:18 — Compare Quick Capture Input glow vs custom drawer shadow path.
10:24 — Adopt Quick Capture-style Input glow on task title; tune border `primary/40`, ring `2px` @ `/40`.
10:34 — Promote that focus chrome to global Input + Textarea.
10:38 — Fix hover-while-focused clearing the indigo border (`hover:focus-visible:` keep border/ring).

## Decisions
- Global field focus = Soft Indigo `border-primary/40` + `ring-2` + `ring-ring/40` (150ms). Hover only lightens fill; must not change border/ring while focused.
- Prefer one shared Input/Textarea path over per-panel custom box-shadow glows.

## Next
- Finish and smoke-test right-drawer time AM/PM picker branch; commit focus + drawer changes together when ready.
- Optional: append decision-log entry for global field focus if treating as product chrome rule.
