# 2026-07-26

## Overview
- Done: Today responsive shell shipped. Soft Indigo field focus global. Right-drawer pickers merged. Silent writing autosave for reflection + notes. Notes + kanban typewriter pad and edge-gap caret settle (shared helper). Kanban card editing UX elevated with primary border + shadow. Notes/kanban typing gaps dogfooded and verified.
- Blockers: None.

## Timeline
08:33 — Ship Today responsive shell: 70%/50% nav stages, fixed Timeline scroll.
08:34 — Merge `tweak/today-responsive-timeline`; note merge hash in docs.
09:55 — Task Details title focus vs description glow mismatch (investigation).
10:00 — Iterate writing-field hover/focus (avoid darken, double glow, white flash).
10:12 — Apply same writing-field treatment to Notes search.
10:18 — Compare Quick Capture Input glow vs custom drawer shadow path.
10:24 — Adopt Quick Capture-style Input glow on task title; tune border `primary/40`, ring `2px` @ `/40`.
10:34 — Promote that focus chrome to global Input + Textarea.
10:38 — Fix hover-while-focused clearing the indigo border (`hover:focus-visible:` keep border/ring).
11:00 — Commit/merge right-drawer + focus + developer-log to `main`.
12:00 — Smooth writing: reflection no longer disables fields while saving; notes autosave no longer rewinds cursor/text from stale server replies.
12:04 — Notes editor typewriter scroll: half-height bottom pad (Notes page + drawer share MarkdownEditor).
12:30 — Kanban column half-height pad while editing/compose; shared caret settle for notes + cards.
12:34 — Fix Backspace: only top-edge gap (not mid-viewport); mirror caret Y for soft wraps; edge gap 128px.
12:38 — Slim helper: one settle API, drop preview pad + unused exports/aliases.
14:00 — Kanban card editing UX: add primary/40 border + shadow-overlay when editing, surface-5 background for elevation.

## Decisions
- Global field focus = Soft Indigo `border-primary/40` + `ring-2` + `ring-ring/40` (150ms). Hover only lightens fill; must not change border/ring while focused.
- Prefer one shared Input/Textarea path over per-panel custom box-shadow glows.
- Writing UX follows task description: local text wins while editing; autosave is silent and must not disable inputs or clobber newer local content.
- Notes/kanban typing comfort = half-height bottom pad (scrollbar at mid) + edge-gap settle only; no auto-center while typing. Backspace mirrors bottom gap at the top.
- Kanban card editing elevation = primary/40 border + shadow-overlay + surface-5 background for visual hierarchy when editing.

## Next
- Optional: decision-log entry for global field focus / silent autosave writing rule.
